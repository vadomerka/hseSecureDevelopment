apiVersion: v1
kind: Service
metadata:
  name: task-app-service
  labels:
    app: task-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: task-app
